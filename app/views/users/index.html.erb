<div class="container">
	<div class="row">
		<!-- サイドバー -->
		<div class="col-xs-3">
			<h2>User info</h2>
			<%= render 'users/profile', user: @user %>

			<h2>New book</h2>
			<%= render 'books/newform', book: @book %>
		</div>
					<!--users一覧 -->
		<div class="col-xs-9">
			<h2>Users</h2>
			<%= render 'index',  users: @users %>
		</div>


