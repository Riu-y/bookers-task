<div class="row">
	<div class="col-md-2"></div>
	</div>
	<div class="col-md-5 col-md-offset-3">

	<h2>Editing Comment</h2>
	<%= form_for([@book_comment.book,@book_comment]) do |f| %>
		<div class="field row">
			<%= f.label :Comment %>
			<%= f.text_area :comment, class: "col-xs-12 book_title" %>
		</div>

		<div class="actions row">
			<%= f.submit class: "btn btn-primary col-xs-5" %>
		</div>
	<% end %><br>
	<%= link_to "Show", book_path(@book_comment.book), class: "book_#{@book_comment.book.id}" %><a> | </a>
	<%= link_to "Back", books_path, class: "back" %>
	</div>


